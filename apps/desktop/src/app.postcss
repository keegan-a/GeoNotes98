@import './lib/styles/theme.css';

:root {
  font-family: 'Segoe UI', 'Roboto', system-ui, -apple-system, sans-serif;
  background-color: rgb(var(--gn-color-surface));
  color: rgb(var(--gn-color-text));
}

body {
  min-height: 100vh;
  margin: 0;
  background-color: rgb(var(--gn-color-surface));
}
